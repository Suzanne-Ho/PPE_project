import { useState, useEffect } from 'react'
import Head from 'next/head'
import Link from 'next/link'
import { ChevronRightIcon } from '@heroicons/react/20/solid'
import Layout from '../../components/Layout.js'
import { useSupabaseClient } from '@supabase/auth-helpers-react'

export default function Contacts() {
  const [reports, setContacts] = useState([])
  const supabase = useSupabaseClient()
  useEffect(() => {
    (async () => {
      let { data, error, status } = await supabase
        .from('reports')
        .select(`*`)
      setContacts(data)
    })()
  }, [supabase])
  return (
    <Layout>
      <Head>
        <title>Edubet - Report list</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <h1 className='flex justify-center mb-4 text-4xl font-extrabold tracking-tight leading-none md:text-5xl lg:text-6xl text-neutralTitle pb-8'>
        Liste des messages de rapport
      </h1>
      <div className="not-prose -my-2 -mx-4 overflow-x-auto sm:-mx-6 lg:-mx-8">
        <div className="inline-block min-w-full py-2 align-middle md:px-6 lg:px-8">
          <div className="overflow-hidden shadow ring-1 ring-black ring-opacity-5 md:rounded-lg">
            <table className="min-w-full divide-y divide-onNeutralBg">
              <thead className="bg-neutralBg">
                <tr>
                  <th scope="col" className="px-3 py-3.5 text-left text-sm font-semibold text-onNeutralBg">
                    Personne signalée
                  </th>
                  <th scope="col" className="px-3 py-3.5 text-left text-sm font-semibold text-onNeutralBg">
                    Signalé par
                  </th>
                  <th scope="col" className="px-3 py-3.5 text-left text-sm font-semibold text-onNeutralBg">
                    Message
                  </th>
                </tr>
              </thead>
              <tbody className="divide-y divide-onNeutralBg bg-neutralBg">
                {reports.map((report) => (
                  <tr key={report.email}>
                    <td className="whitespace-nowrap px-3 py-4 text-sm text-onNeutralBg">{report.author}</td>
                    <td className="whitespace-nowrap px-3 py-4 text-sm text-onNeutralBg">{report.report_person}</td>
                    <td className="whitespace-nowrap px-3 py-4 text-sm text-onNeutralBg">{report.reason}</td>
                    <td>
                      <Link
                        className={"w-5 h-5 block bg-slate-200 hover:bg-onPrimaryBg hover:text-white rounded-full"}
                        href={`/admin/reports/${report.id}`}
                      >
                        <ChevronRightIcon className="h-5 w-5 " aria-hidden="true" />
                      </Link>
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </Layout>
  )
}
